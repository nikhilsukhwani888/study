# study

to compile and debug/run
Ctrl+Shft+P ->  Tasks: Run Build Tasks(or directly do Ctrl+Shft+B) -> F5

or

in bash -> ./build.sh -> this will compile and run the program if there are no errors available.